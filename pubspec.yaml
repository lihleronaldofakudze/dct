name: dreams_come_true
description: Dreams Come True Online Shopping Application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.0
  provider: ^5.0.0
  firebase_core: ^1.3.0
  firebase_auth: ^1.4.1
  firebase_storage: ^10.0.3
  cloud_firestore: ^2.2.2
  animated_splash_screen: ^1.1.0
  carousel_slider: ^4.0.0
  image_picker: ^0.8.3+3
  pin_code_fields: ^7.3.0
  flutter_spinkit: ^5.0.0
  share_plus: ^2.1.4
  square_in_app_payments: ^1.7.1
  path_provider: ^2.0.3
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
     - images/